import { TransformarOracionPipe } from './transformar-oracion.pipe';

describe('TransformarOracionPipe', () => {
  it('create an instance', () => {
    const pipe = new TransformarOracionPipe();
    expect(pipe).toBeTruthy();
  });
});
